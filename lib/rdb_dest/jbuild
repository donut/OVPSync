(jbuild_version 1)

(library
  ((name rdb_dest)
   (libraries (lib sync
               batteries caqti caqti-dynload caqti-lwt cohttp.lwt lwt re uri))
   (preprocess (pps (ppx_fields_conv lwt.ppx)))
  ))