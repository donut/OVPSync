(jbuild_version 1)

(library
  ((name rdb_dest)
   (libraries (lib logger sync
               batteries caqti caqti-driver-mariadb caqti-lwt 
               cohttp-lwt cohttp-lwt-unix 
               lwt re uri))
   (preprocess (pps (ppx_fields_conv lwt_ppx)))
  ))